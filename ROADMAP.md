# TS Swagger Version Roadmap

本文档用于把 TS Swagger 从当前的开发者工具，逐步推进到可以稳定对外发布的产品。

## 版本节奏

- 小版本保持可独立交付，每个版本解决一个明确产品问题。
- 优先顺序建议为：先降低首次体验门槛，再补齐本地保存闭环，然后稳定核心生成器、扩展诊断和复杂 Schema 能力。
- 每个版本都应包含必要的文档更新和最小验证手段。

## v0.4.0 - 首次体验优化

### 目标

让用户不连接真实后端，也能在首次打开页面时立即理解产品价值。

### 范围

- 首页增加“一键体验 Demo”入口。
- 内置一份典型 OpenAPI 示例文档。
- Demo 覆盖接口列表、接口详情、TypeScript 类型生成、复制代码。
- 未安装浏览器扩展时，明确说明哪些能力可用、哪些能力不可用。
- README 增加截图、快速开始和 Demo 使用说明。

### 验收标准

- 新用户打开 Web UI 后，不输入任何后端地址也能完成一次完整体验。
- Demo 不依赖外部网络服务。
- 未安装扩展时，页面仍能查看 Demo 和生成类型。

## v0.5.0 - 本地接口库 + JSON 导出

### 目标

让用户可以把当前接口数据导出为 JSON 文件，也可以保存到浏览器本地库，之后无需后端和扩展即可离线打开。

### 范围

- 顶部栏增加“保存到本地”和“导出 JSON”。
- 导出统一数据包，包含原始 OpenAPI、接口分组、接口列表、完整 URL、TS 代码片段、来源信息和生成配置。
- 使用 IndexedDB 保存本地接口库。
- 首页展示本地保存的接口文档列表。
- 支持打开、删除和再次导出本地记录。
- 相同文档内容重复保存时更新旧记录，不新增重复数据。
- 本地记录按浏览器 origin 隔离，文档中说明清理站点数据会删除。

### 验收标准

- Demo 可以保存到本地，重复保存只更新记录。
- 首页能看到本地记录，并可打开到 `?local=<id>`。
- 本地记录打开后不请求后端、不依赖扩展，仍能浏览接口和查看 TS 类型。
- JSON 导出包含 `openapi`、`groups`、`apis`、`tsCode`、`source`、`generatorOptions`。

## v0.5.1 - 未安装扩展体验优化

### 目标

让用户没安装浏览器扩展时仍能清楚知道哪些能力可用，真实 Swagger 地址失败时能得到明确原因和下一步。

### 范围

- 统一扩展检测状态，支持重新检测和页面回到可见时自动重检。
- 首页展示无扩展可用能力、需要扩展能力、下载扩展、重新检测和 Demo 入口。
- 外部文档在无扩展时先尝试原生 `fetch`，失败后展示跨域/内网诊断。
- 文档工作台错误态提供下载扩展、重新检测、试用 Demo、返回首页。
- 网络调试页中，未安装扩展时禁用 proxyFetch 模式操作，原生 fetch 仍可使用。

### 验收标准

- 未安装扩展时 Demo、同源文档、本地接口库仍可正常使用。
- 跨域/内网文档失败时，用户能看到需要扩展的原因和可执行操作。
- 安装或启用扩展后，点击“重新检测”可更新状态。

## v0.5.2 - 导入 JSON + 本地库管理器

### 目标

让用户可以导入 ts-swagger 导出的 JSON 或普通 OpenAPI/Swagger JSON，保存到浏览器本地接口库并离线打开。

### 范围

- 首页使用独立“本地接口库”管理弹窗，承载导入、打开、导出和删除。
- 导入 ts-swagger 导出包时保留原始数据包。
- 导入普通 OpenAPI/Swagger JSON 时标准化为 `TsSwaggerExport` 后保存。
- 导入成功后写入 IndexedDB，并自动跳转到 `?local=<id>`。
- IndexedDB 仍沿用 fingerprint 去重策略，重复导入只更新记录。

### 验收标准

- 合法 ts-swagger 导出包和普通 OpenAPI/Swagger JSON 都可导入并打开。
- 非法 JSON 或非 OpenAPI JSON 会给出明确中文错误。
- 未安装扩展时仍可导入并打开本地文档。

## v0.6.0 - 复杂 Schema 支持

### 目标

提升对真实后端复杂 Swagger/OpenAPI 文档的兼容能力，减少退化为 `any` 的情况。

### 范围

- 支持 `nullable`。
- 支持 `additionalProperties`。
- 支持 `oneOf`、`anyOf`、`allOf`。
- 支持 `multipart/form-data`。
- 支持 `application/x-www-form-urlencoded`。
- response 支持展示或选择多个状态码。
- 增强模型引用链路，避免循环引用导致异常。

### 验收标准

- 复杂 Schema fixture 能生成合理 TypeScript 类型。
- 常见组合 Schema 不再直接退化为 `any`。
- 循环引用不会导致生成器崩溃。

## v0.7.0 - 扩展安装与诊断产品化

### 目标

把浏览器扩展相关问题从“用户自己排查”，变成产品内可识别、可解释、可处理的诊断流程。

### 范围

- UI 展示主站版本和扩展版本。
- 检测扩展是否安装。
- 检测扩展版本是否过低。
- 增加扩展权限说明。
- 请求失败时展示诊断面板。
- 展示实际探测过的 swagger-config URL。
- 区分网络不可达、超时、HTTP 错误、JSON 格式错误、OpenAPI 文档格式错误。

### 验收标准

- 用户可以在页面内明确知道扩展是否可用。
- 请求失败时，错误信息包含可行动的下一步建议。
- swagger-config 自动探测过程对用户可见。

## v0.8.0 - 项目配置与团队使用

### 目标

让 TS Swagger 从个人工具升级为团队内可共享、可复用的接口工作台。

### 范围

- 支持导入和导出配置。
- 支持项目级预设，包括 host、service、生成选项、命名策略。
- 支持通过 URL 分享当前文档和 API 位置。
- 最近访问文档支持分组、重命名、删除。
- CLI 增加配置校验命令，例如 `ts-swagger config check`。
- README 增加团队使用建议。

### 验收标准

- 用户可以迁移或共享常用配置。
- 同一个团队可以使用统一配置进入同一份 API 文档。
- 配置错误可以通过 CLI 或 UI 得到明确提示。

## v0.9.0 - 请求调试增强

### 目标

打通“看文档、生成类型、调试请求、复制代码”的完整接口开发流程。

### 范围

- 从 API 文档自动生成请求参数表单。
- 根据 schema 生成默认 request body。
- 支持请求历史。
- 支持环境变量，例如 dev、test、prod baseUrl。
- 支持真实响应和 OpenAPI schema 对比。
- 支持复制 fetch、axios、curl 请求代码。

### 验收标准

- 用户可以从接口详情页直接发起一次请求。
- 请求代码可以复制到项目中使用。
- 响应和 schema 不一致时，页面能给出清晰提示。

## v1.0.0 - 公开发布版

### 目标

完成对外发布所需的产品包装、工程保障和发布流程。

### 范围

- 正式发布 npm CLI。
- GitHub Pages 正式部署 Web UI。
- 稳定扩展 release 流程。
- 完整产品 README。
- 完整 changelog。
- 基础 E2E 测试。
- 明确 license。
- 增加贡献指南。
- 完成品牌、logo、截图和示例项目整理。

### 验收标准

- 用户可以通过 README 完成安装、体验和排障。
- CLI、Web UI、扩展三者版本关系清晰。
- release 流程可重复执行。
- 项目具备对外推广的最小工程质量保障。

## 推荐开发顺序

1. v0.4.0：先让用户能快速试用。
2. v0.5.0：把已加载的接口数据沉淀为本地库和可导出的 JSON。
3. v0.7.0：解决扩展安装和请求失败这类高频卡点。
4. v0.6.0：补真实项目中最常见的复杂 Schema。
5. v0.8.0：支持团队共享和项目配置。
6. v0.9.0：增强请求调试闭环。
7. v1.0.0：整理发布、文档和工程质量。
