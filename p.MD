
```bazaar
帮定一个技术方案。功能是用于展示 http请求的数据（proxyFetch），我期待它Chrome浏览器一样 越像越好 因为开发已经习惯使用Chrome调试请求了。

ui交互和Chrome浏览器越像越好，有请求列表，点击后展示详情，详情是标签栏，标签有‘标头’、‘核载’、‘预览’，‘响应’、‘启动器’、‘时间’、‘Cookie’。

需要注意的是
1.有没有开源可用的组件（不造重复轮子）
2.如果是自己做的话 现有的数据类型（ProxyResponse）支持做到和Chrome一样吗？
```


```bazaar
Home页面增加浏览器搜索记录功能。
1.用户输入ip搜索成功后，将ip添加到搜索记录中。
2.搜索记录最多展示10条，最近搜索的在最上面。
3.用户可在搜索下拉选项中进行重命名和删除。
```

```bazaar
优化首页Home.tsx，我希望用户一进来 如果URL上没有搜索参数（ip） 就能看到一个清晰的界面，具体优化点如下：

当URL上没有搜索参数时，展示一个欢迎界面，包含以下内容：
   - 显示一个大的ip搜索输入框，提示用户输入ip地址进行搜索。
   - 如果用户没有安装浏览器拓展，展示一个明显的提示信息，指导用户安装浏览器拓展以获得更好的使用体验。
   - 最近的搜索记录列表，最多展示10条，最近搜索的在最上面。
```
